import React from 'react';
import PropTypes from 'prop-types';

import cn from './ScoreControl.module.scss';

export const ScoreControl = (props) => {
  return <div className={cn.scoreControl}>
    
  </div>;
};

ScoreControl.propTypes = {};
